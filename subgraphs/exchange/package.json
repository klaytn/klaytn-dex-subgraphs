{
  "name": "exchange",
  "version": "0.1.0",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ klaytn-subgraph/exchange",
    "create-local": "graph create --node http://localhost:8020/ klaytn-subgraph/exchange",
    "remove-local": "graph remove --node http://localhost:8020/ klaytn-subgraph/exchange",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 klaytn-subgraph/exchange"
  }
}
