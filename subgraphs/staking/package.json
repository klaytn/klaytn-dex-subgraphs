{
  "name": "staking",
  "version": "0.1.0",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create --node http://localhost:8020/ klaytn-subgraph/staking",
    "remove-local": "graph remove --node http://localhost:8020/ klaytn-subgraph/staking",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 klaytn-subgraph/staking",
    "create-remote": "graph create klaytn-subgraph/staking",
    "remove-remote": "graph remove klaytn-subgraph/staking",
    "deploy-remote": "graph deploy klaytn-subgraph/staking"
  }
}
