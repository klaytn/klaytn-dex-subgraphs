{
  "name": "exchange",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ klaytn/exchange",
    "create-local": "graph create --node http://localhost:8020/ klaytn/exchange",
    "remove-local": "graph remove --node http://localhost:8020/ klaytn/exchange",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 klaytn/exchange"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
