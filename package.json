{
  "name": "klaytn-subgraph",
  "private": true,
  "version": "0.1.0",
  "license": "GPL-3.0-or-later",
  "workspaces": [
    "subgraphs/**/*"
  ],
  "scripts": {
    "codegen": "lerna run codegen",
    "build": "lerna run build --",
    "create-local": "lerna run create-local",
    "remove-local": "lerna run remove-local",
    "deploy-local": "lerna run deploy-local -- --version-label 0.0.1",
    "create-remote": "lerna run create-remote",
    "remove-remote": "lerna run remove-remote",
    "deploy-remote": "lerna run deploy-remote -- --version-label 0.0.1"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.29.2",
    "@graphprotocol/graph-ts": "^0.26.0",
    "lerna": "^4.0.0"
  }
}
